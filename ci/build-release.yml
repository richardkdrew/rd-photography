platform: linux

image_resource:
  type: docker-image
  source: {repository: richardkdrew/docker-node-grunt-bower, tag: "latest"}

inputs:
  - name: photography
  - name: version
  - name: build_artifacts

outputs:
  - name: release

run:
  path: photography/ci/build-release.sh
